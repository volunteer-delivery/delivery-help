version: 0.2

env:
  shell: bash
  variables:
    FRONTEND_IMAGE: delivery-help-frontend-arm-staging
    FRONTEND_TAG: $CR_HOST/$CR_USERNAME/$FRONTEND_IMAGE:latest
    BACKEND_IMAGE: delivery-help-backend-arm-staging
    BACKEND_TAG: $CR_HOST/$CR_USERNAME/$BACKEND_IMAGE:latest

phases:
  install:
    runtime-versions:
      docker: 19

  pre_build:
    commands:
      - echo Logging in to GitHub Registry...
      - echo $CR_PASSWORD | docker login $CR_HOST -u $CR_USERNAME --password-stdin
      - docker pull $FRONTEND_TAG
      - docker pull $BACKEND_TAG

  build:
    commands:
      - echo Building frontend...
      - >
        docker build \
          -t $FRONTEND_IMAGE:latest \
          -f deployment/staging/frontend/codebuild.Dockerfile \
          --cache-from $FRONTEND_TAG \
          --build-arg FRONTEND_API_URL=$FRONTEND_API_URL \
          --build-arg FRONTEND_SOCKET_URL=$FRONTEND_SOCKET_URL \
          --build-arg FRONTEND_IP=$FRONTEND_IP \
          --build-arg FRONTEND_DOMAIN=$FRONTEND_DOMAIN \
          .
      - docker tag $FRONTEND_IMAGE:latest $FRONTEND_TAG

      - echo Building backend...
      - >
        docker build \
          -t $BACKEND_IMAGE:latest \
          -f deployment/staging/backend/Dockerfile \
          --cache-from $BACKEND_TAG \
          .
      - docker tag $BACKEND_IMAGE:latest $BACKEND_TAG

      - echo Pushing docker images...
      - docker push $FRONTEND_TAG
      - docker push $BACKEND_TAG
